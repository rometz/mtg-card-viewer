export function fetchBooster(set) {
    return async (dispatch) => {
        dispatch({type: "LOADING_BOOSTER"});
        /* create function in CardsController to reflect a route*/
        /* create a mtg::api controller? */
    }
}