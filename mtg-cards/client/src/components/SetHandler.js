import React from 'react';

const SetHandler = props => {

    return(
        <ul>
            <li id="set-code">{this.props.code}</li>
            <li id="set-name">{this.props.name}</li>
        </ul>
    );
};

export default SetHandler;